{
  "lane": "C",
  "updated": "2026-09-13T04:10:00Z",
  "done": [
    "rule pack: 17 rules, thresholds in the units the standard is written in, the quoted sentence behind each, and every other number the section states",
    "human verification ledger binding rule id, section, threshold and unit, so editing a threshold switches its check off",
    "cli rules review: prints the section and asks for the number back",
    "tier 1 checks against MeasurementProvider: route width, 180 turn, passing space, turning space, door width, counter height, counter approach, exit path",
    "403.5.1's exception evaluated through Lane B's route_run_below rather than failed closed",
    "403.5.2 from Lane B's four measurements and the pack's four thresholds",
    "the five things a scan cannot see, as one specific request each",
    "owner-facing copy for every finding, tested against section 2 of the plan",
    "findings with stable ids, dedupe across legs, needs_another_look downgraded to a request",
    "assess(): one pass producing an Assessment plus what it could not answer",
    "findings_for(graph, scenario): the entrypoint Lane D's assess stage calls",
    "TypeSafe router: closed action set, schema generated from the contract, fail-closed validation of malformed, contradictory and truncated output",
    "labelled local policy for when TypeSafe is unavailable",
    "fix agent: bounded search over slides and quarter turns, hard constraints from plan section 10, one tested relaxation when it runs out",
    "a sealed route is a shortfall of the whole requirement, not an unknown, and widening is not offered where there is no gap to widen",
    "the ask box: eight kinds of question about the room, answered from the measured model with a locus to fly the camera to",
    "every model call through OpenRouter with the provider pinned and retention denied per request",
    "the loop: one handler per action and no default, so an invalid answer authorizes nothing",
    "acceptance gate: completes, no lost coverage, no new failures, strictly improves",
    "32 labelled cases and 7 scorers, with per-case results on disk and in Weave when configured",
    "Weave tracing that runs with or without an account",
    "tier 2 checks: door maneuvering clearance (404.2.4) read from the stricter end of the table, and protruding objects (307) from the leading edge and the projection off the wall",
    "tier 3: dining surface height (902.3) with 226.1's share; reach range (308) is a photo request because a scan does not identify an operable part",
    "a verified rule with no check behind it reports itself, so the one silent pass left in the lane is guarded",
    "the rule pack says whether a finding is cleared by moving things or by changing a fixture, so the router never sends the fix agent to make a table shorter",
    "38 labelled cases across all three tiers, 7 scorers, every one at 1.000",
    "368 tests"
  ],
  "in_progress": null,
  "blocked_on": null,
  "ready_for_others": [
    "standardphysics_agents.findings_for(graph, scenario) -> list[Finding], and RULEPACK_VERSION",
    "standardphysics_agents.assess(graph, scenario, measure) -> Pass, with .problems, .questions and .unevaluated",
    "standardphysics_agents.propose_fix(...) -> FixOutcome, with the proposal, the gate result and the relaxation",
    "standardphysics_agents.run_loop(graph, scenario, measure, router) -> list[LoopStep]",
    "standardphysics_agents.ask(text, graph, scenario, measure) -> Answer, for Lane D's ask box",
    "standardphysics_agents.evaluate() -> EvaluationResult, and accepts(before, after) for the gate",
    "load_pack().as_contract_pack(ledger) -> contracts.RulePack, for Lane D's report"
  ],
  "needs_human": [
    "BLOCKING THE WHOLE REPORT: read each rule's section and confirm its number. python -m standardphysics_agents.cli rules review --by \"<name>\". Thirteen tier 1 rules, a few minutes. Until then every check is off and findings_for returns nothing.",
    "A second person re-checks each citation: cli rules second-check <id> --by \"<name>\"",
    "Pin the CBC Chapter 10 egress section for exit_path and confirm the occupant load for a shop this size. An accessibility professional or the Palo Alto Building Division.",
    "Confirm Lane B's two disclosed approximations in ADA 403.5.2: the pivot's smaller horizontal extent read as the element width, and geometric turn detection at about 122 degrees",
    "TYPESAFE_API_KEY and the quickstart's base URL and request path into .env, as TYPESAFE_BASE_URL. Without them the loop runs the local policy and says so, and the TypeSafe track comes out of the pitch.",
    "OPENROUTER_API_KEY in .env with zero data retention on the account and a credit limit. Without it the ask box matches keywords instead of reading the question, and says so.",
    "WANDB_API_KEY, WANDB_ENTITY and WANDB_PROJECT in .env, or tracing and evaluation stay local",
    "Schedule the accessibility professional",
    "One line in pytest.ini or Lane C's 263 tests never run on master: docs/handoffs/C-to-D.md item 3"
  ]
}
